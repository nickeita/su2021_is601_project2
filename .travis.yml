language: python
python:
  - 3.9

services:
  - docker

before_install:
  - docker build -t python-stats-calc:0.0
  - docker run -d python-stats-calc:0.0
  - docker ps -a
  - docker run python-stats-calc:0.0